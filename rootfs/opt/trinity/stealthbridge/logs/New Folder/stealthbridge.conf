# StealthBridge Configuration
MODE=passive               # Options: off, passive, glyph-only, full-stealth
BRIDGE_INTERFACE=eth0      # Interface to apply stealth rules
GLYPH_REQUIRED=yes         # Require glyph handshake before accepting inbound
ROLLING_MASK=yes           # Enable IP rotation / MAC randomization
